# Code Extraction Analysis: README & Intended Structure

## Document Status

- **README.md** – Analyzed.
- **UoB_EIS_Technical_Design_Document.docx** – **Analyzed** (text extracted from project root). Section 4 (Component Design) and Section 7 (Infrastructure) were compared to the extracted code layout.

---

## 1. README.md vs Extracted Layout

### 1.1 Paths Referenced in README

| README reference | Expected path | Extracted? | Notes |
|------------------|---------------|------------|--------|
| CloudFormation template | `infrastructure/main.yaml` | ✅ | Present at root. |
| Build output | `dist/` | N/A | Generated by `npm run build`, not in code.txt. |
| Frontend app | `frontend/` | ✅ | Present with `package.json`, `src/`, etc. |
| Lambda handlers | dashboard, kpis, alerts, reports | ✅ | `src/handlers/*.handler.ts` (including alert-processor). |
| Unit tests | `npm test`, Jest | ✅ | `tests/setup.ts`, `tests/unit/**/*.test.ts`. |
| Scripts | `scripts/` (package, seed-data, create-admin) | ✅ | All at root `scripts/`. |
| Env config | `.env` from `.env.example` | ❌ | `.env.example` not in code.txt. |
| Infra parameters | `infrastructure/parameters/` (dev, staging, prod) | ❌ | Folder/files not in code.txt. |

### 1.2 npm Scripts vs Extracted Files

- **`npm run build`** → `tsc` → expects `src/` and `tsconfig.json` at root. ✅ Both present.
- **`npm run test`** → Jest with `roots: ['<rootDir>/tests']`, `setupFilesAfterEnv: ['<rootDir>/tests/setup.ts']`. ✅ `tests/` at root matches.
- **`npm run package`** → `node scripts/package-lambdas.js`. ✅ `scripts/package-lambdas.js` at root.
- **`npm run seed-data`** → `ts-node scripts/seed-data.ts`. ✅ `scripts/seed-data.ts` at root.
- **`npm run create-admin`** → `ts-node scripts/create-admin-user.ts`. ✅ `scripts/create-admin-user.ts` at root.
- **`npm run deploy:dev`** → uses `infrastructure/main.yaml`. ✅ Present.

**Conclusion:** All paths and scripts referenced in the README that exist in the extracted code are in the correct places.

---

## 2. Appendix Structure (code.txt) vs Extracted Layout

The appendix in `code.txt` describes a **frontend-centric** tree (e.g. `e2e/`, `.github/`, `scripts/` under `frontend/`). The **section headers** in `code.txt` that we used for extraction use **root-level** paths for those items. Extraction followed those section paths.

### 2.1 Backend (root-level) – All Correct

| Intended / Appendix | Extracted location | Match |
|---------------------|--------------------|--------|
| `src/types/` | `src/types/` | ✅ |
| `src/constants/` | `src/constants/` | ✅ |
| `src/utils/` | `src/utils/` | ✅ |
| `src/middleware/` | `src/middleware/` | ✅ |
| `src/repositories/` | `src/repositories/` | ✅ |
| `src/services/` | `src/services/` | ✅ |
| `src/handlers/` | `src/handlers/` | ✅ |
| `infrastructure/main.yaml` | `infrastructure/main.yaml` | ✅ |
| `tests/` | `tests/` | ✅ |
| `scripts/` (seed-data, create-admin, deploy, package-lambdas) | `scripts/` at root | ✅ |

### 2.2 Frontend – All Correct

| Appendix path | Extracted path | Match |
|---------------|----------------|--------|
| `frontend/package.json`, `tsconfig.json`, `vite.config.ts`, `tailwind.config.js` | Same | ✅ |
| `frontend/src/main.tsx`, `App.tsx` | Same | ✅ |
| `frontend/src/types/index.ts` | Same | ✅ |
| `frontend/src/components/dashboard/*` | Same | ✅ |
| `frontend/src/components/alerts/*` | Same | ✅ |
| `frontend/src/components/common/*` | Same | ✅ |
| `frontend/src/hooks/*` | Same | ✅ |
| `frontend/src/services/*` | Same | ✅ |
| `frontend/src/context/*` | Same | ✅ |
| `frontend/src/test/*` | Same | ✅ |

### 2.3 Root vs Appendix (e2e, CI, scripts, playwright)

| Item | Appendix in code.txt | Extracted (from section headers) | Verdict |
|------|----------------------|-----------------------------------|---------|
| `e2e/` | Under `frontend/` | At **root** `e2e/` | ✅ Prefer root for monorepo; matches section headers. |
| `.github/workflows/` | Under `frontend/` | At **root** `.github/workflows/` | ✅ Standard for repo-wide CI. |
| `scripts/deploy.sh`, `scripts/package-lambdas.js` | Under `frontend/scripts/` | At **root** `scripts/` | ✅ README uses `npm run package` at root; root is correct. |
| `playwright.config.ts` | Under `frontend/` | At **root** | ✅ Common when e2e lives at root; can stay or move to `frontend/` if you prefer. |

So: extraction matches the **section headers** in `code.txt` and matches **README** expectations. Any difference is between the appendix diagram (frontend-centric) and the actual section paths (root-centric); the extracted layout is consistent and appropriate.

---

## 3. Files in Appendix But Not in code.txt (Not Extracted)

These appear in the appendix or README but were **not** in `code.txt`, so they were not created by the extractor:

| File / folder | Notes |
|---------------|--------|
| `frontend/index.html` | Entry point for Vite; usually needed for frontend. |
| `frontend/src/components/auth/ProtectedRoute.tsx` | Auth component. |
| `frontend/src/config/aws.config.ts` | AWS/frontend config. |
| `frontend/src/styles/globals.css` | Global styles. |
| `infrastructure/parameters/dev.json` (and staging/prod) | README mentions these for env-specific config. |
| `.env.example` | README uses for env setup. |

These are **missing by design** (not present in the dump), not misplaced.

---

## 4. UoB_EIS_Technical_Design_Document.docx vs Extracted Layout

The TDD (Section 4 – Component Design, Section 7 – Infrastructure) specifies the following structure. Comparison with extracted files:

### 4.1 Backend (TDD Section 4.2–4.4, 7.2)

| TDD specification | Extracted path | Match |
|-------------------|----------------|--------|
| `src/handlers/dashboard.handler.ts` | `src/handlers/dashboard.handler.ts` | ✅ |
| `src/handlers/kpis.handler.ts` | `src/handlers/kpis.handler.ts` | ✅ |
| `src/handlers/alert-processor.handler.ts` | `src/handlers/alert-processor.handler.ts` | ✅ |
| `src/services/dashboard.service.ts` | `src/services/dashboard.service.ts` | ✅ |
| `src/services/kpi.service.ts` | `src/services/kpi.service.ts` | ✅ |
| `src/repositories/kpi.repository`, `alert.repository` | `src/repositories/*.repository.ts` | ✅ |
| `src/middleware/auth.middleware` | `src/middleware/auth.middleware.ts` | ✅ |
| `src/utils/response.util`, `logger.util`, `validation.util` | `src/utils/*.util.ts` | ✅ |
| `infrastructure/main.yaml` | `infrastructure/main.yaml` | ✅ |

Backend layout matches the TDD.

### 4.2 Frontend (TDD Section 4.1)

| TDD specification | Extracted | Notes |
|-------------------|-----------|--------|
| `frontend/src/components/dashboard/` – DashboardLayout, KPICard, TrendChart, FilterPanel | ✅ All present | Correct folder. |
| `frontend/src/components/dashboard/` – **AlertBanner** | ✅ Present as `frontend/src/components/alerts/AlertBanner.tsx` | TDD lists AlertBanner under dashboard; code uses a separate `alerts/` folder. Both valid; extraction matches code.txt. |
| `frontend/src/components/common/` – Header, Sidebar, **LoadingSpinner** | Header, Sidebar ✅; **LoadingScreen.tsx** (not LoadingSpinner) | Same role; name differs (LoadingScreen vs LoadingSpinner). |
| `frontend/src/components/kpis/` – EnrollmentKPIs, FinancialKPIs, AcademicKPIs | ❌ Not in code.txt | TDD-only; not extracted (not in source dump). |
| `frontend/src/hooks/` – useAuth, useDashboard, useKPIs, useAlerts | ✅ All present (+ useTheme) | Correct. |
| `frontend/src/services/` – api.service, **auth.service** | api.service ✅; dashboard, kpi, alert services ✅; no **auth.service.ts** | Auth handled via useAuth hook / AuthContext; no separate auth.service in code.txt. |
| `frontend/src/types/index.ts` | ✅ | Correct. |
| `frontend/src/utils/` – formatters.ts, validators.ts | ❌ Not in code.txt | TDD-only; not in source dump. |

So: **all files that exist in code.txt are in the correct folders.** The only differences are (1) AlertBanner under `alerts/` instead of `dashboard/`, (2) LoadingScreen vs LoadingSpinner, and (3) components/services/utils that the TDD describes but were not included in code.txt.

### 4.3 Infrastructure (TDD Section 7)

- TDD expects `infrastructure/main.yaml` with Cognito, API Gateway, Lambda (Dashboard, KPIs, Alerts, Reports, DataIngestion, AlertProcessor), DynamoDB, S3, SNS, CloudWatch, CloudFront.
- Extracted: `infrastructure/main.yaml` at project root. ✅

### 4.4 Test paths (TDD Section 12)

- TDD references `jest.mock('../../../src/repositories/kpi.repository')` and handler tests under `tests/`.
- Extracted: `tests/setup.ts`, `tests/unit/services/kpi.service.test.ts`, `tests/unit/handlers/dashboard.handler.test.ts`. ✅

---

## 5. Summary

| Check | Result |
|-------|--------|
| Backend `src/` layout | ✅ Matches README, TDD, and code.txt sections. |
| Frontend `frontend/` layout | ✅ Matches TDD Section 4.1 and code.txt (AlertBanner in `alerts/`; TDD lists under dashboard—both valid). |
| `infrastructure/`, `tests/`, `scripts/` | ✅ Correct and consistent with README and TDD. |
| `e2e/`, `.github/`, `playwright.config.ts` at root | ✅ Matches section headers and common practice. |
| README script paths | ✅ All point to existing, correctly placed files. |
| TDD (UoB_EIS_Technical_Design_Document.docx) | ✅ Analyzed; extracted layout aligns with TDD Sections 4 and 7. |

**Overall:** Code files from `code.txt` are in the correct folders per README, the Technical Design Document, and the section headers in `code.txt`. Minor differences: (1) TDD puts AlertBanner under dashboard; code uses `alerts/`. (2) TDD mentions LoadingSpinner; code has LoadingScreen. (3) Some TDD-only items (e.g. `frontend/src/components/kpis/`, `frontend/src/utils/`, `auth.service.ts`) were not in `code.txt` and were not created. (4) Appendix in code.txt shows e2e/CI/scripts under frontend; section headers use root—extraction followed section headers.
